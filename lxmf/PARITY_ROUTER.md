# Parity: python/LXMF/LXMRouter.py

# Status
- The router now matches the Python lifecycle: startup config, propagation/peer data persistence (`locally_delivered`, `locally_processed`, `node_stats`), control paths, signal handling, and the routing jobs that run in the background.

# TODO
- None.
